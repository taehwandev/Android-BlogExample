<?xml version="1.0" encoding="utf-8"?>
<MotionScene xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:motion="http://schemas.android.com/apk/res-auto">

    <Transition
        motion:constraintSetEnd="@+id/end"
        motion:constraintSetStart="@+id/start"
        motion:duration="1000" />

    <ConstraintSet android:id="@+id/start">

        <Constraint android:id="@+id/view_login">
            <Transform android:translationY="0dp" />
            <PropertySet android:alpha="1" />
        </Constraint>

        <Constraint android:id="@+id/view_login_success">
            <Transform android:translationY="100dp" />
            <PropertySet android:alpha="0" />
        </Constraint>

        <Constraint
            android:id="@+id/btn_action"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:layout_marginTop="16dp"
            android:layout_marginEnd="10dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/view_login" />
    </ConstraintSet>

    <ConstraintSet android:id="@+id/end">
        <Constraint android:id="@+id/view_login">
            <Transform android:translationY="100dp" />
            <PropertySet android:alpha="0" />
        </Constraint>

        <Constraint android:id="@+id/view_login_success">
            <Transform android:translationY="0dp" />
            <PropertySet android:alpha="1" />
        </Constraint>

        <Constraint
            android:id="@+id/btn_action"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:layout_marginTop="16dp"
            android:layout_marginEnd="10dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/view_login_success" />
    </ConstraintSet>
</MotionScene>