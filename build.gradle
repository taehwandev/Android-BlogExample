// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    repositories {
        mavenCentral()
        google()
        jcenter()
    }

    ext {
        compileSdkVersionInfo = 29
        buildToolsVersionInfo = "29.0.3"
        minSdkVerisonInfo = 24
        targetSdkVersionInfo = 29

        kotlin_version = '1.3.72'

        appcompatVersion = '1.1.0'
        activityVersion = '1.2.0-alpha04'
        materialVersion = '1.1.0'
        constraintLayoutVersion = '1.1.3'

        coreKtx = '1.2.0'
        lifecycleExtensions = '2.2.0'

        // Rx
        rxandroidVersion = '3.0.0'
        rxjavaVersion = '3.0.3'

        // Retrofit
        // https://github.com/square/retrofit
        retrofitVersion = '2.8.1'
        retrofitRxJavaAdapter = '3.0.0'

        // OkHttp
        // https://github.com/square/okhttp
        okhttpVersion = '4.6.0'

        // Image loader
        // https://github.com/bumptech/glide
        glideVersion = '4.11.0'
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:3.6.3'
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

// Kobis API key는 local.properties에 "kobisApiKey="생성한 키""를 등록하면 됩니다
ext {
    Properties properties = new Properties()
    properties.load(project.rootProject.file('local.properties').newDataInputStream())
    kobisApiKey = properties.getProperty('kobisApiKey', "\"\"")
}

allprojects {
    repositories {
        mavenCentral()
        google()
        jcenter()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}